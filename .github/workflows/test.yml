name: firstworkflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: |
        echo "hello world"
        echo "Welcome to the world of github actions"
      
    
